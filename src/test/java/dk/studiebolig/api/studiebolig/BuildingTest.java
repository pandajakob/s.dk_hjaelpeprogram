package dk.studiebolig.api.studiebolig;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class BuildingTest {
    // Building b = new Building();
/*
    @Test
    void testRankings() {
        b.ranking.incrementC();
        b.ranking.incrementA();
        b.ranking.incrementA();
        b.ranking.incrementB();
        b.ranking.incrementB();
        b.ranking.incrementB();

        assertEquals(1, b.getRanking('C'));
        assertEquals(2, b.getRanking('A'));
        assertEquals(3, b.getRanking('B'));
    }

    @Test
    void testName() {
        b.setName("test");
        assertEquals("test", b.getName());
    }

    @Test
    void testAddress() {
        b.setDesc_address("address");
        assertEquals("address", b.getDesc_address());
    }

    @Test
    void testPK() {
        b.setPk(123);
        assertEquals(123, b.getPk());
    }
    @Test
    void latitudeAndLongitude() {
        b.setLatitude("6789");
        b.setLongitude("1234");
        assertEquals("6789", b.getLatitude());
        assertEquals("1234", b.getLongitude());
    }


 */
}